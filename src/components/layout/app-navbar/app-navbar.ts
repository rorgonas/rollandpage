import { defineComponent } from 'vue';
import ThemeToggle from '@/components/ui/theme-toggle/theme-toggle.vue';

export default defineComponent({
  name: 'AppNavbar',
  components: {
    ThemeToggle,
  },
});
